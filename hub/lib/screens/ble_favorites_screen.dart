import 'package:flutter/material.dart';

class BleFavorites extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Favorites'),
    );
  }
}
